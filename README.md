# 🔍 Мониторинг конкурентов - AI Ассистент

MVP мультимодального приложения для анализа конкурентов с поддержкой текста и изображений.

## 📋 Содержание

- [Описание](#-описание)
- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Использование](#-использование)
  - [Веб-интерфейс](#веб-интерфейс)
  - [Desktop приложение](#desktop-приложение)
  - [API](#api)
- [Сборка Desktop приложения](#-сборка-desktop-приложения)
- [Структура проекта](#-структура-проекта)
- [Решение проблем](#-решение-проблем)
- [FAQ](#-faq)

## 🎯 Описание

**Мониторинг конкурентов - AI Ассистент** - это комплексное решение для анализа конкурентов с использованием искусственного интеллекта. Приложение позволяет:

- Анализировать текстовый контент конкурентов
- Оценивать визуальные материалы (баннеры, сайты, упаковку)
- Автоматически парсить и анализировать веб-страницы
- Хранить историю всех анализов

Приложение состоит из трех компонентов:
- **Backend API** (FastAPI) - серверная часть с AI интеграцией
- **Веб-интерфейс** - браузерная версия приложения
- **Desktop приложение** - нативное Windows приложение на PyQt6

## ✨ Возможности

### 📝 Анализ текста
- Структурированная аналитика конкурентов
- Выявление сильных и слабых сторон
- Определение уникальных предложений
- Персонализированные рекомендации по улучшению стратегии

### 🖼️ Анализ изображений
- Описание визуального контента
- Маркетинговые инсайты
- Оценка визуального стиля (0-10)
- Оценка дизайна и архитектуры
- Потенциал для анимации/визуализации
- Рекомендации по улучшению

### 🌐 Парсинг сайтов
- Автоматическое извлечение контента (title, H1, первый абзац)
- Интеллектуальный анализ извлеченного контента
- Структурированный отчет с рекомендациями

### 📚 История запросов
- Хранение последних 10 запросов
- Просмотр истории с датами и типами запросов
- Возможность очистки истории

## 🔧 Требования

### Обязательные
- **Python 3.8+** (рекомендуется 3.11+)
- **OpenAI API ключ** - получите на [platform.openai.com](https://platform.openai.com/api-keys)
- **Интернет соединение** для работы с OpenAI API

### Опциональные
- **Chrome/Chromium браузер** - для использования Selenium парсера (автоматически скачивается)
- **Windows 10+** - для сборки и запуска desktop приложения

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd cursor_ai_agent_conkurenty
```

### 2. Установка зависимостей

```bash
# Установка основных зависимостей
pip install -r requirements.txt

# Для desktop приложения (опционально)
cd desktop
pip install -r requirements.txt
cd ..
```

### 3. Установка PyInstaller (для сборки desktop приложения)

```bash
pip install pyinstaller
```

## ⚙️ Настройка

### 1. Создание файла `.env`

Создайте файл `.env` в корневой директории проекта:

```env
# OpenAI настройки (ОБЯЗАТЕЛЬНО)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini

# API настройки
API_HOST=0.0.0.0
API_PORT=8000

# История запросов
HISTORY_FILE=history.json
MAX_HISTORY_ITEMS=10

# Парсер настройки
PARSER_TIMEOUT=10
PARSER_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# Selenium настройки (опционально)
USE_SELENIUM=false
SELENIUM_TIMEOUT=15
SELENIUM_HEADLESS=true
SELENIUM_WAIT_TIME=3

# URL конкурентов для мониторинга (через запятую)
COMPETITOR_URLS=https://example1.com,https://example2.com
```

### 2. Получение OpenAI API ключа

1. Перейдите на [platform.openai.com](https://platform.openai.com)
2. Войдите в свой аккаунт или создайте новый
3. Перейдите в раздел **API Keys**
4. Нажмите **Create new secret key**
5. Скопируйте ключ и вставьте в `.env` файл

**⚠️ Важно:** Никогда не публикуйте ваш API ключ в публичных репозиториях!

### 3. Настройка моделей OpenAI

Рекомендуемые модели:
- **gpt-4o-mini** - быстрая и экономичная (рекомендуется)
- **gpt-4o** - более точная, но дороже
- **gpt-4-turbo** - альтернатива для сложных задач

## 🚀 Использование

### Запуск Backend сервера

```bash
# Из корневой директории проекта
python run.py
```

Сервер запустится на `http://localhost:8000`

Доступные эндпоинты:
- **Веб-интерфейс:** http://localhost:8000
- **API документация (Swagger):** http://localhost:8000/docs
- **ReDoc документация:** http://localhost:8000/redoc
- **Health check:** http://localhost:8000/health

### Веб-интерфейс

1. Запустите backend сервер (`python run.py`)
2. Откройте браузер и перейдите на `http://localhost:8000`
3. Используйте интерфейс для:
   - **Анализа текста** - вставьте текст конкурента и получите структурированный анализ
   - **Анализа изображений** - загрузите изображение (PNG, JPG, JPEG, GIF, WEBP)
   - **Парсинга сайтов** - введите URL для автоматического анализа
   - **Просмотра истории** - посмотрите все предыдущие запросы

### Desktop приложение

#### Запуск из исходников

```bash
# Убедитесь, что backend сервер запущен
python run.py

# В другом терминале, из директории desktop/
cd desktop
python main.py
```

#### Использование собранного .exe

1. Запустите backend сервер (`python run.py`)
2. Запустите `desktop/dist/CompetitorMonitor.exe`
3. Используйте интерфейс аналогично веб-версии

**Примечание:** Desktop приложение подключается к `http://localhost:8000` по умолчанию. Убедитесь, что backend запущен перед использованием desktop приложения.

### API

#### Примеры использования API

##### Анализ текста

```bash
curl -X POST "http://localhost:8000/analyze_text" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Ваш текст для анализа здесь..."
  }'
```

##### Анализ изображения

```bash
curl -X POST "http://localhost:8000/analyze_image" \
  -F "file=@path/to/image.png"
```

##### Парсинг сайта

```bash
curl -X POST "http://localhost:8000/parse_demo" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com"
  }'
```

##### Получение истории

```bash
curl -X GET "http://localhost:8000/history"
```

##### Очистка истории

```bash
curl -X DELETE "http://localhost:8000/history"
```

Полная документация API доступна по адресу `/docs` после запуска сервера.

## 🔨 Сборка Desktop приложения

### Требования для сборки

- Python 3.8+
- PyInstaller (`pip install pyinstaller`)
- Все зависимости из `desktop/requirements.txt`

### Процесс сборки

```bash
# Перейдите в директорию desktop
cd desktop

# Запустите скрипт сборки
python build.py
```

После успешной сборки файл будет находиться в `desktop/dist/CompetitorMonitor.exe`

### Очистка артефактов сборки

```bash
cd desktop
python build.py clean
```

Это удалит:
- `build/` директорию
- `dist/` директорию
- `*.spec` файлы
- `__pycache__/` директории

### Размер и требования

- **Размер .exe файла:** ~36-40 MB
- **Требования:** Windows 10+, не требует установки Python
- **Зависимости:** Backend сервер должен быть запущен

## 📁 Структура проекта

```
cursor_ai_agent_conkurenty/
├── backend/                      # Backend API (FastAPI)
│   ├── __init__.py
│   ├── config.py                 # Конфигурация приложения
│   ├── main.py                    # FastAPI приложение и эндпоинты
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py             # Pydantic модели для валидации
│   └── services/
│       ├── __init__.py
│       ├── openai_service.py      # Интеграция с OpenAI API
│       ├── parser_service.py      # Парсинг веб-страниц
│       └── history_service.py      # Управление историей запросов
│
├── desktop/                       # Desktop приложение (PyQt6)
│   ├── main.py                   # Главное окно приложения
│   ├── api_client.py             # HTTP клиент для API
│   ├── styles.py                 # Стили интерфейса
│   ├── build.py                  # Скрипт сборки .exe
│   ├── requirements.txt          # Зависимости desktop приложения
│   ├── dist/
│   │   └── CompetitorMonitor.exe  # Собранное приложение
│   └── build/                     # Временные файлы сборки
│
├── web/                          # Веб-интерфейс
│   ├── index.html                # HTML разметка
│   ├── style.css                 # Стили
│   └── app.js                    # JavaScript логика
│
├── .env                          # Конфигурация (создается вручную)
├── .env.example                  # Пример конфигурации
├── requirements.txt              # Основные зависимости
├── run.py                        # Скрипт запуска сервера
├── history.json                  # История запросов (генерируется)
├── README.md                     # Документация
└── LICENSE                       # Лицензия
```

## 🔧 Решение проблем

### Backend не запускается

**Проблема:** Ошибка при запуске `python run.py`

**Решения:**
1. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
2. Убедитесь, что порт 8000 свободен
3. Проверьте наличие файла `.env` с правильным `OPENAI_API_KEY`
4. Проверьте версию Python: `python --version` (должна быть 3.8+)

### Desktop приложение не подключается к серверу

**Проблема:** Ошибка "Не удалось подключиться к серверу"

**Решения:**
1. Убедитесь, что backend сервер запущен (`python run.py`)
2. Проверьте, что сервер доступен на `http://localhost:8000`
3. Откройте браузер и перейдите на `http://localhost:8000/health` - должен вернуться статус 200
4. Проверьте файрвол - возможно, он блокирует подключение

### Ошибка при загрузке изображений

**Проблема:** "Неподдерживаемый тип файла"

**Решения:**
1. Убедитесь, что файл имеет расширение: `.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`
2. Проверьте, что файл не поврежден
3. Попробуйте другой формат изображения

### История пуста

**Проблема:** История не отображается

**Решения:**
1. Убедитесь, что вы выполнили хотя бы один запрос (анализ текста, изображения или парсинг)
2. Проверьте файл `history.json` в корне проекта - он должен содержать данные
3. Попробуйте обновить историю, нажав кнопку "Обновить историю"

### Ошибки OpenAI API

**Проблема:** Ошибки при обращении к OpenAI API

**Решения:**
1. Проверьте правильность API ключа в `.env`
2. Убедитесь, что у вас есть баланс на счету OpenAI
3. Проверьте лимиты API - возможно, превышен rate limit
4. Проверьте интернет соединение

### Проблемы со сборкой .exe

**Проблема:** Ошибки при сборке desktop приложения

**Решения:**
1. Убедитесь, что PyInstaller установлен: `pip install pyinstaller`
2. Проверьте, что все зависимости установлены: `pip install -r desktop/requirements.txt`
3. Убедитесь, что вы находитесь в директории `desktop/` при запуске `build.py`
4. Очистите старые артефакты: `python build.py clean`

### Логирование ошибок

Desktop приложение создает файл `desktop/app.log` с подробными логами ошибок. Если возникают проблемы, проверьте этот файл.

## ❓ FAQ

### Как изменить порт сервера?

Измените `API_PORT` в файле `.env`:
```env
API_PORT=8080
```

### Можно ли использовать другие AI модели?

Да, измените `OPENAI_MODEL` и `OPENAI_VISION_MODEL` в `.env`. Поддерживаются все модели OpenAI, которые поддерживают нужные функции.

### Как увеличить лимит истории?

Измените `MAX_HISTORY_ITEMS` в `.env`:
```env
MAX_HISTORY_ITEMS=50
```

### Можно ли использовать приложение без интернета?

Нет, приложение требует подключения к интернету для работы с OpenAI API.

### Как изменить адрес сервера в desktop приложении?

Отредактируйте `BASE_URL` в файле `desktop/api_client.py`:
```python
BASE_URL = "http://your-server-ip:8000"
```

### Поддерживаются ли другие операционные системы?

- **Backend и Web:** Работают на Windows, Linux, macOS
- **Desktop приложение:** Собрано только для Windows. Для других ОС нужно изменить `build.py` и пересобрать

### Как обновить приложение?

1. Получите последнюю версию из репозитория
2. Обновите зависимости: `pip install -r requirements.txt --upgrade`
3. Пересоберите desktop приложение, если используете .exe

### Безопасность API ключа

**⚠️ Важно:**
- Никогда не коммитьте файл `.env` в Git
- Не публикуйте API ключ в публичных местах
- Используйте переменные окружения в production
- Регулярно ротируйте API ключи

## 🛠️ Технологии

- **Backend:** FastAPI, Python 3.8+
- **AI:** OpenAI GPT-4o / GPT-4o-mini
- **Frontend (Web):** HTML5, CSS3, JavaScript (Vanilla)
- **Frontend (Desktop):** PyQt6
- **Парсинг:** BeautifulSoup4, httpx, Selenium (опционально)
- **Валидация:** Pydantic
- **Сборка:** PyInstaller

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если у вас возникли проблемы или вопросы:
1. Проверьте раздел [Решение проблем](#-решение-проблем)
2. Проверьте файл логов `desktop/app.log` (для desktop приложения)
3. Откройте issue в репозитории проекта

---

**Создано с использованием OpenAI GPT-4o**

**Версия:** 1.0.0
