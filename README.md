# 🔍 Мониторинг конкурентов - AI Ассистент

MVP мультимодального приложения для анализа конкурентов с поддержкой текста и изображений.

## 📋 Описание

Приложение помогает анализировать конкурентов, предоставляя:
- **Анализ текста** - структурированная аналитика (сильные/слабые стороны, уникальные предложения, рекомендации)
- **Анализ изображений** - маркетинговые инсайты и оценка визуального стиля
- **Парсинг сайтов** - автоматическое извлечение и анализ контента с веб-страниц
- **История запросов** - хранение последних 10 запросов

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```env
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini

API_HOST=0.0.0.0
API_PORT=8000

# Selenium настройки (опционально)
USE_SELENIUM=false
SELENIUM_TIMEOUT=15
SELENIUM_HEADLESS=true
SELENIUM_WAIT_TIME=3

# URL конкурентов для мониторинга (через запятую)
COMPETITOR_URLS=https://example1.com,https://example2.com
```

**Важно:** 
- Получите API ключ OpenAI на [platform.openai.com](https://platform.openai.com/api-keys)
- Для использования Selenium установите `USE_SELENIUM=true` (требует установки Chrome/Chromium)
- Selenium автоматически скачает ChromeDriver через webdriver-manager

### 3. Запуск приложения

```bash
# Запуск сервера
python run.py
```

Приложение будет доступно по адресу: `http://localhost:8000`

- Веб-интерфейс: `http://localhost:8000`
- API документация: `http://localhost:8000/docs`
- ReDoc документация: `http://localhost:8000/redoc`

## 📁 Структура проекта

```
cursor_ai_agent_conkurenty/
├── backend/                 # FastAPI backend
│   ├── __init__.py
│   ├── config.py            # Конфигурация
│   ├── main.py              # FastAPI приложение
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py       # Pydantic модели
│   └── services/
│       ├── __init__.py
│       ├── openai_service.py    # Интеграция с OpenAI
│       ├── parser_service.py    # Парсинг веб-страниц
│       └── history_service.py   # Управление историей
├── web/                     # Веб-интерфейс
│   ├── index.html
│   ├── style.css
│   └── app.js
├── .env.example             # Пример конфигурации
├── requirements.txt         # Зависимости
├── run.py                   # Скрипт запуска
├── README.md                # Документация
└── history.json             # История запросов (генерируется)
```

## 🔧 Функциональность

### Анализ текста (`/analyze_text`)
Принимает текст и возвращает:
- Сильные стороны конкурента
- Слабые стороны
- Уникальные предложения
- Рекомендации по улучшению стратегии

### Анализ изображений (`/analyze_image`)
Принимает изображение (баннер, сайт, упаковка) и возвращает:
- Описание изображения
- Маркетинговые инсайты
- Оценку визуального стиля (0-10)
- Рекомендации

### Парсинг сайтов (`/parse_demo`)
Принимает URL и:
- Извлекает title, h1, первый абзац
- Автоматически анализирует контент
- Возвращает структурированный анализ

### История (`/history`)
- Просмотр последних 10 запросов
- Очистка истории

## 🛠️ Технологии

- **Backend:** FastAPI, Python 3.8+
- **AI:** OpenAI GPT-4o / GPT-4o-mini
- **Frontend:** HTML5, CSS3, JavaScript (Vanilla)
- **Парсинг:** BeautifulSoup4, httpx, Selenium (опционально)
- **Данные:** Pydantic для валидации

## 📖 API Документация

Полная документация доступна по адресу `/docs` после запуска сервера.

## ⚠️ Требования

- Python 3.8 или выше
- OpenAI API ключ
- Интернет соединение для работы с OpenAI API
- Chrome/Chromium браузер (для использования Selenium, опционально)
- Selenium автоматически скачает ChromeDriver при первом использовании

## 📝 Лицензия

MIT License

---

**Создано с использованием OpenAI GPT-4o**

